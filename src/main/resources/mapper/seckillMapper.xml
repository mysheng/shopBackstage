<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mysheng.office.mapper.SeckillMapper">
    <select id="selectIndexSeckill" resultType="com.mysheng.office.model.Seckill">
        SELECT * FROM seckill where end_date <![CDATA[ < ]]> #{date}
    </select>

</mapper>